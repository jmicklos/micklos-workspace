List all active PARA projects with `/resume-project` commands, ready to paste into separate Claude Code sessions.

## Steps

1. List all folders in `01 – Projects/` (excluding `00_backlog`).
2. For each folder, output a line: `/resume-project [folder name]`
3. Print the total count.
4. Ask if the user also wants to include backlog projects from `01 – Projects/00_backlog/`.

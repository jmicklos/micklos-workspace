List all active PARA project names, one per line, ready to paste into the PARA Session Launcher.

## Steps

1. List all folders in `01 – Projects/` (excluding `00_backlog`).
2. Output each folder name on its own line — just the name, no commands or formatting.
3. Print the total count.
